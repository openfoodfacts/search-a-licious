name: "💥 Auto-Label Merge Conflicts on PRs"
on:
  push:
  pull_request_target:
    types: [synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - uses: eps1lon/actions-label-merge-conflict@0273be72a0bbd58fcd71d0d6c02c209b50d1e5e1  # v3.0.3 (2025-01-06)
        continue-on-error: true  # Don’t mark PRs as failing because this errs
        with:
          dirtyLabel: "💥 Merge Conflicts"
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          retryMax: 5
          retryAfter: 5000
