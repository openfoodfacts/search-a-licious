import {css} from 'lit';

/**
 * The styles for icon elements from off styles
 */
export const iconStyle = css`
  .icon {
    display: inline-block;
    fill: currentColor;
    margin-left: 0.25em;
    margin-right: 0.25em;
    overflow: hidden;
    width: 1em;
    height: 1em;
  }
`;
