import {css} from 'lit';

export const iconStyle = css`
  .icon {
    display: inline-block;
    fill: currentColor;
    margin-left: 0.25em;
    margin-right: 0.25em;
    overflow: hidden;
    width: 1em;
    height: 1em;
  }
`;
